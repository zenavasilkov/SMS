<UserControl x:Class="UniversityApp.WPF.Views.GroupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:UniversityApp.WPF.Views"
             Background="Transparent">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Groups}"
                  SelectedItem="{Binding SelectedGroup, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  Background="#353340"
                  Foreground="White"
                  FontSize="14"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  Style="{StaticResource DarkDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" />
                <DataGridTextColumn Header="Специальность" Binding="{Binding Specialty}" />
                <DataGridTextColumn Header="Сокращение" Binding="{Binding Abbreviation}" />
                <DataGridTextColumn Header="Факультет" Binding="{Binding Faculty}" />
                <DataGridTextColumn Header="Год формирования" Binding="{Binding YearOfAdmission}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
