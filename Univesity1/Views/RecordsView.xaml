<UserControl x:Class="UniversityApp.WPF.Views.RecordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Records}"
                  SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  Background="#353340"
                  Foreground="White"
                  FontSize="14"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  Style="{StaticResource DarkDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" />
                <DataGridTextColumn Header="Зачётка" Binding="{Binding RecordBookNumber}" />
                <DataGridTextColumn Header="Предмет" Binding="{Binding Subject.Designation}" />
                <DataGridTextColumn Header="Семестр" Binding="{Binding Semester}" />
                <DataGridTextColumn Header="Оценка" Binding="{Binding Grade}" />
                <DataGridCheckBoxColumn Header="Курсовая" Binding="{Binding IsCourseProject}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
